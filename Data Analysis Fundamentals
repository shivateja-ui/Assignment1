#Create New Conditional Column
= IF(ship_mode = "Same Day", (sales - profit) * (1 + 0.2),
  IF(ship_mode = "First Class", (sales - profit) * (1 + 0.1),
    IF(ship_mode = "Standard Class", (sales - profit) * (1 + 0.05),
      0
    )
  )
)
